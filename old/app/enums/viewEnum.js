module.exports = {
    instanceView: { selector: '#instances-view' },
    formView: { selector: '#form-instance-view' },
    connectView: { selector: '#connect-view' },
    profileView: { selector: '#profile-view' },
}